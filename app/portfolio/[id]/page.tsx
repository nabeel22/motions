import Link from '@/app/link';import {notFound} from 'next/navigation';import {CTA} from '../../site';import {projects} from '../../data';
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const i=Number(id);if(!/^\d$/.test(id)||!projects[i])notFound();const p=projects[i];return <><section className="wrap details-hero"><div><Link className="eyebrow" href="/portfolio">← All Projects</Link><span className="eyebrow">{p.category} / Selected concept</span><h1>{p.name}</h1><p>{p.text}</p><Link href="https://calendly.com/mskiani2277/kianimotions" className="pill">Book a call <span aria-hidden="true">↗</span></Link></div><img src={'/assets/project-'+i+'.webp'} alt={p.name+' design'}/></section><section className="wrap prose"><h2>The creative direction</h2><p>{p.text} This concept explores how visual hierarchy, distinctive color, and thoughtful interactions can make a digital product feel more engaging and easier to use.</p><h2>Designed around the experience</h2><p>A clear structure gives the content room to breathe. Strong imagery creates an immediate impression, while a consistent visual language connects the experience from beginning to end.</p><Link className="text-link" href={'/portfolio/'+((i+1)%projects.length)}>Next Project: {projects[(i+1)%projects.length].name} →</Link></section><CTA/></>}
